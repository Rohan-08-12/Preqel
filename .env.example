# --- Supabase project settings -> API ---
NEXT_PUBLIC_SUPABASE_URL=
# Publishable key (sb_publishable_...) — from the Connect dialog or Settings -> API Keys
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# --- Supabase project settings -> Database -> Connection string (URI, "Session" pooler mode / port 5432) ---
# Used only by the ingest/normalize/aggregate scripts for bulk writes.
SUPABASE_DB_URL=
